version: 2
jobs:
  setup:
    docker:
      - image: circleci/node:10.16.0
    steps:
      - checkout
      - run: yarn install --exact
      - run: yarn bootstrap
  build:
    docker:
      - image: circleci/node:10.16.0
    steps:
      - run: ls
      - run: yarn build
      - run: yarn link
workflows:
  version: 2
  setup_and_build:
    jobs:
      - setup
      - build:
          requires:
            - setup