{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "for-the-north",
  "scripts": {
    "help": "yarn lerna --help",
    "bootstrap": "yarn lerna bootstrap",
    "lerna-init": "yarn lerna init --independent",
    "lerna-add": "yarn lerna add"
  },
  "devDependencies": {
    "lerna": "3.15.0"
  }
}
