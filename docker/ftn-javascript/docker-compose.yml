version: "3.7"
services:
  ftn-js-blog:
    image: node:alpine
    env_file:
      - .env
    build:
      context: ${ROOT_DIR}
      dockerfile: ./docker/ftn-javascript/Dockerfile
    ports:
      - "3001:3001"
    volumes:
      - .:/ftn-javascript