import Layout from 'components/styled/layout'
const About = () => (
  <Layout>
    <div>
      FTN was created with the purpose to simplify and empathize with those that
      think that JavaScript should not be as complicated as the web resonates.
    </div>
  </Layout>
)

export default About